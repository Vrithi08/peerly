spring.application.name=campus-platform

# Server Configuration
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/campus_platform}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:vrithika}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Security Configuration
jwt.secret=${JWT_SECRET:campus_platform_super_secret_key_vrithi_2024_very_long_key}
jwt.expiration=${JWT_EXPIRATION:604800000}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dlbkkr8fl}
cloudinary.api-key=${CLOUDINARY_API_KEY:529875123318667}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:i5BGgYdj1W2k8nTouEQZhDvnZ5g}

# File Upload Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.vrithi.campus_platform=INFO